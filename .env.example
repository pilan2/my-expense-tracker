# Supabase Connect > ORM(Prisma) 탭에서 확인 가능
DATABASE_URL=""
DIRECT_URL=""

# Google Cloud Console > 사용자 인증 정보 (OAuth 클라이언트)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# openssl rand -base64 32 로 생성
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# 로그인 허용할 단일 계정 (소유자 본인 이메일)
ALLOWED_EMAIL=""

# Vercel Cron이 /api/cron/update-shipped 호출 시 이 값으로 인증 (openssl rand -base64 32로 생성)
CRON_SECRET=""
