# My Expense Tracker — PRD

## 1. 개요 (Overview)

**해결하려는 문제 (Problem Statement)**
수기로 소비내역을 기록할 때 발생하는 번거로움과, 기존 방식(엑셀/노트 등)의 불편한 UI.

**최종 목표 (Objective)**
아래 10가지 항목을 입력하면 구매 내역이 자동으로 저장·집계되고, 이후 판매 등으로 보유 목록에 변동이 생기면 목록과 판매 금액까지 함께 관리할 수 있는 시스템을 만든다.

| # | 항목 | 비고 |
|---|---|---|
| 1 | 장르 | 필수 |
| 2 | 캐릭터 | 필수 |
| 3 | 물품 종류 | 필수 |
| 4 | 수량 | 필수 |
| 5 | 가격 | 필수 |
| 6 | 이후 해외배송비 존재 여부 | 필수 |
| 7 | 제작한 사람 | 선택 |
| 8 | 공구 개최한 사람 | 선택 |
| 9 | 현물 여부 | 필수 |
| 10 | 예상 발송일 | 현물이 아닐 때만 |

**핵심 지표 (Success Metrics)**
- 소유자의 모바일·PC 환경 모두에서 문제없이 동작
- UI가 충분히 깔끔하고 잘 정돈되어 가독성이 높음

---

## 2. 핵심 요구사항 (Core Requirements & MVP)

**유저 스토리 (User Story)**
사용자는 언제 어디서든 편하게 소비내역을 추가할 수 있어야 한다. 이를 위해 프로그램은 상시 동작해야 하고, 앱처럼 사용 가능해야 하며, 가능하면 웹(홈페이지)에서도 동작해야 한다.

**필수 기능 (Must-Have)**

| 구분 | 기능 |
|---|---|
| 입력 (클릭형) | 장르 / 캐릭터 / 물품 종류 / 수량 / 해외배송비 존재 여부 / 현물 여부 / 예상 발송일을 클릭으로 선택 |
| 입력 (텍스트형) | 가격 / 제작한 사람 / 공구 개최한 사람을 타이핑으로 입력 |
| 소비 집계 | 장르·캐릭터 등 카테고리별 소비 금액과 총 소비 금액 확인 |
| 판매 처리 | 보유 물품 리스트에서 물품·수량 선택 후 판매 금액 입력 |
| 판매 집계 | 카테고리별 판매 금액과 총 판매 금액 확인 |
| 수동 수정 | 리스트 항목을 수기로 직접 수정 가능 |
| D-Day 확인 | 아직 배송되지 않은(현물이 아닌) 물품의 발송까지 D-Day 확인 |
| 자동 전환 | D-Day가 지난 물품은 현물 상태로 업데이트 |

---

## 3. 기술 및 환경 (Tech Stack & Loadout)

| 영역 | 선택 기술 | 선택 이유 |
|---|---|---|
| 개발 언어·프레임워크 | Next.js (React 18+) + TypeScript | 하나의 코드베이스로 웹/모바일 반응형 UI를 모두 처리 |
| 스타일링 | Tailwind CSS | 빠르게 깔끔하고 정돈된 UI 구성 |
| 앱 설치 지원 | PWA (next-pwa 또는 Serwist) | 모바일 홈 화면에 아이콘 추가, 네이티브 앱처럼 설치·사용 가능 |
| 배포 | Vercel | 상시 동작(24/7), HTTPS 기본 제공, GitHub main 브랜치 push 시 자동 빌드/배포(CI/CD) |
| 데이터베이스 | Supabase (PostgreSQL, 무료 티어) | 별도 서버 관리 없이 상시 접근 가능한 클라우드 DB |
| ORM | Prisma | 스키마·쿼리 관리 |
| 배치 작업 | Vercel Cron Jobs | 매일 1회, "예상 발송일 경과 → 현물 전환" 자동 처리 |
| 버전 관리 | Git / GitHub | 소스 버전 관리 |
| 인증 | NextAuth.js(Auth.js) + Google OAuth | 별도 비밀번호 관리 없이 소셜 로그인만으로 인증. 허용된 단일 Google 계정(소유자 이메일)만 화이트리스트로 접근 허용, 그 외 계정은 로그인 거부 |
| 민감정보 관리 | Vercel 환경변수(.env) | DB 접속 정보, OAuth 클라이언트 키 등은 저장소에 커밋하지 않고 환경변수로 관리 |

---

## 4. 시스템 아키텍처 및 로직 (System Logic)

### 4.1 데이터 모델

**`Item` (구매 품목)**

| 필드 | 설명 |
|---|---|
| id | 고유 식별자 |
| 장르 / 캐릭터 / 물품 종류 | 카테고리 정보 |
| 수량 | 보유 수량 |
| 가격 | 구매 가격 |
| 해외배송비 존재 여부 | boolean |
| 제작한 사람 (선택) | 텍스트 |
| 공구 개최한 사람 (선택) | 텍스트 |
| 현물 여부 | boolean |
| 예상 발송일 | 현물이 아닐 때만 사용 |
| 등록일 / 수정일 | 타임스탬프 |

**`Sale` (판매 내역)**

| 필드 | 설명 |
|---|---|
| id | 고유 식별자 |
| item_id | `Item` 참조(FK) |
| 판매 수량 | - |
| 판매 금액 | - |
| 판매일 | - |

> 관계: `Item` 1 : N `Sale` — 한 품목이 여러 번에 걸쳐 부분 판매되는 경우도 표현 가능.

### 4.2 핵심 로직 흐름

1. **구매 등록** — 클릭형 필드(장르/캐릭터/물품 종류/수량/해외배송비 여부/현물 여부/예상 발송일)와 텍스트형 필드(가격/제작한 사람/공구 개최한 사람)를 입력받아 `Item` 레코드 생성.
2. **소비 집계** — 대시보드 진입 시 `Item`을 카테고리(장르/캐릭터 등) 기준으로 집계(GROUP BY)하여 카테고리별 합계와 전체 합계를 서버에서 계산 후 응답, 클라이언트는 렌더링만 담당.
3. **판매 처리** — 보유 품목 리스트에서 Item과 수량을 선택하고 판매 금액을 입력하면 `Sale` 레코드 생성. 판매 수량만큼 잔여 수량에 반영하며, 전량 판매 시 "판매 완료"로 표시하되 이력은 삭제하지 않음.
4. **판매 집계** — `Sale`을 연결된 `Item`의 카테고리 기준으로 집계하여 카테고리별·전체 판매액 계산.
5. **수동 수정** — 모든 `Item`/`Sale` 필드는 상세 화면에서 직접 수정 가능(자동 계산값 제외).
6. **D-Day 계산 및 자동 전환** — 현물이 아닌 Item은 `예상 발송일 − 오늘`로 D-Day를 계산해 노출. 매일 실행되는 Cron Job이 예상 발송일이 지난 Item을 조회해 현물 여부를 자동으로 true로 갱신(사용자가 수동으로 먼저 바꿔도 무방).

### 4.3 화면 흐름 (초안)

```
로그인 화면
   └─ 대시보드 (카테고리별/전체 소비·판매 합계, D-Day 임박 품목 하이라이트)
        ├─ 품목 목록 (필터/검색)
        │     └─ 품목 상세 · 수정
        ├─ 품목 등록 폼
        └─ 판매 등록 폼
```

---

## 5. 마일스톤 및 실행 계획 (Execution Timeline)

| Phase | 내용 | 기간(추정) |
|---|---|---|
| 0. 프로젝트 셋업 | Next.js/TypeScript/Tailwind 초기화, Supabase 프로젝트 생성 및 Prisma 연동, Google OAuth 앱 등록 및 NextAuth 로그인(화이트리스트 포함) 구현, Vercel 배포 파이프라인 연결 | 3~5일 |
| 1. 품목 등록/조회 MVP | `Item` 데이터 모델 확정, 등록 폼(클릭형+텍스트형) 구현, 품목 목록/상세 화면, 수동 수정 기능 | 1주 |
| 2. 소비 집계 대시보드 | 카테고리별·전체 소비 금액 집계 API 및 대시보드 UI 구현 | 3~5일 |
| 3. 판매 관리 | `Sale` 데이터 모델, 판매 등록 폼(품목·수량 선택+금액 입력), 카테고리별·전체 판매 금액 집계 UI | 1주 |
| 4. D-Day 및 현물 자동 전환 | D-Day 표시 로직, Vercel Cron Job으로 예상 발송일 경과 품목 자동 현물 전환 | 3~4일 |
| 5. PWA 및 반응형 마감 | 모바일/PC 반응형 레이아웃 다듬기, PWA 매니페스트·아이콘·설치 배너 적용, 실기기 테스트 | 3~5일 |
| 6. 배포 및 실사용 QA | 실 데이터로 등록·판매·D-Day 시나리오 전체 점검, 버그 픽스 후 정식 사용 시작 | 2~3일 |

> 총 예상 기간: 약 4~5주(개인 프로젝트 기준 추정치). 실제 작업 가능 시간에 따라 조정 가능.
