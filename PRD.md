# My Expense Tracker — 현재 제품 요구사항

## 목적

굿즈·소장품의 구매, 부분/묶음 판매, 배송, 행사 수령을 모바일에서 빠르게 관리한다.
PC와 클라우드 동기화를 전제로 했던 초기 기획을 변경하여, 기기의 로컬 저장을 기본으로 한다.

## 저장과 실행

- 개인 데이터와 사진은 현재 기기의 브라우저 IndexedDB에 저장한다.
- 로그인, Google OAuth, Supabase, 서버 액션, DB 서버가 없어도 핵심 기능을 사용한다.
- 초기 앱 파일 다운로드와 업데이트는 호스팅 서버를 사용한다. 일반 데이터 조회·수정과
  통계 계산은 기기에서 처리하며 다른 기기로 자동 전송하지 않는다.
- 첫 온라인 실행에서 앱 캐시가 완성된 뒤 오프라인 시작·경로 이동·등록·판매·백업을 지원한다.
- 데이터 읽기 실패를 빈 데이터로 취급하지 않는다. 저장 실패는 화면에 표시하고 기존
  저장본을 유지한다. 연결된 레코드와 사진의 변경은 함께 저장하거나 함께 취소한다.

## 기존 기능 유지

| 기능 | 동작 |
|---|---|
| 품목 | 장르, 캐릭터, 선택적 시리즈, 물품 종류, 세부사항, 수량, 단가, 구매일, 배송비, 제작자/공구자, 발송일, 링크, 메모, 사진 |
| 금액 | 만원 단위 입력, 원 단위 저장·집계 |
| 날짜 모름 | 구매일·판매일을 비울 수 있고 전체 합계에는 포함, 날짜별 통계에는 제외 |
| 판매 | 원래 구매 수량은 유지하고 판매 이력으로 잔여 수량 계산, 잔여 수량 초과 판매 거절 |
| 묶음 판매 | 구매 단가 × 판매 수량 비율로 총 판매액 배분 |
| 배송비 | 선택 품목의 수량 비율로 총액 배분, 원 단위 합계 보존 |
| 발송 | 정확한 날짜 또는 월 단위 예정일, 지난 예정일은 배송중 표시, 실제 수령 확인 시에만 현물 전환 |
| 발송 그룹 | 같은 구매일 및 제작자/공구자 기준 그룹화, 그룹 단위 날짜 수정과 품목 추가/제거 |
| 카탈로그 | 추가·삭제·이름 변경·순서 변경, 이름 변경은 연결된 품목 문자열에도 적용 |
| 행사 | 부스별 구매/수령 체크리스트, 품목 연결, 완료 체크, 금액 합계 |
| 통계 | 월별 구매·판매 추이, 장르·캐릭터·제작자 집계와 판매 손익 |
| 사진 | 브라우저 내 회전/자르기 후 로컬 저장 |

날짜 갱신은 자정 예약과 앱 복귀 이벤트로 처리하고 주기적 폴링이나 서버 Cron은 사용하지 않는다.
품목 삭제 시 판매와 연결된 체크리스트도 삭제한다. 카탈로그 삭제는 기존 품목의 문자열을 지우지 않는다.

## 백업과 이전

버전 2 JSON은 품목, 판매, 카탈로그와 순서, 행사, 체크리스트, 발송 그룹, 사진을 모두 포함한다.
백업 불러오기는 형식·날짜·금액·수량·참조 관계·사진을 먼저 검증하고 사용자 확인 후 전체 교체한다.
오류가 있으면 부분 복원하지 않는다. 사진 URL만 있던 이전 백업은 사진을 내려받아 로컬화한다.
이전 백업에 없던 정보의 복구 한계를 표시한다.

기존 클라우드 전체 데이터는 별도 읽기 전용 도구로 내보낸 후 모바일에서 가져온다.
클라우드 데이터 삭제·서비스 종료는 자동화하지 않는다.

## 완료 기준

- 기존 관리 기능이 클라우드 요청 없이 동작한다.
- 오프라인에서도 품목 등록, 판매, 행사 체크, 사진 보기, 백업이 가능하다.
- 새로고침 후 데이터가 유지되고 다른 브라우저에는 노출되지 않는다.
- 탭 간 갱신과 동시 저장 시 데이터 전체가 과거 상태로 덮어써지지 않는다.
- 잘못된 백업, 저장소 오류, 사진 다운로드 실패 시 기존 데이터가 유지된다.
- 타입·린트·프로덕션 빌드·저장소 테스트·모바일 브라우저 테스트를 통과한다.

구현과 이전 절차는 [README](README.md), [로컬 저장 안내](docs/local-storage.md)를 참고한다.
