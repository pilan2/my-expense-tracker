# 작업 인수인계 메모 (Claude Code 세션용)

이 파일은 커밋 메시지나 코드만 봐서는 알 수 없는 것들 — 작업 방식, 이미 시도했다가
되돌린 접근, 코드 스타일 관례 — 을 정리해둔 문서다. 새 Claude Code 세션이 이 프로젝트를
이어받을 때 먼저 읽어두면 좋다. (CLAUDE.md에서 자동으로 불러온다.)

## 1. 작업 방식 (반드시 지킬 것)

- **한 번에 하나씩**: 사용자가 요청 하나(또는 번호 매긴 여러 항목을 한 메시지로)를 주면
  구현하고, 다음 요청까지 스코프를 넓히지 않는다.
- **검증 순서**: 구현 → `npx tsc --noEmit -p .` → `npm run lint` → `npm run build` →
  dev 서버 재시작(포트 3000, curl로 200/307 확인) → 사용자에게 테스트 요청.
- **푸시는 명시적 확인 후에만**: 사용자가 "이대로 푸시해줘" 같은 명확한 승인을 하기 전에는
  절대 커밋·푸시하지 않는다. 구현이 끝났다고 자동으로 푸시하지 말 것.
- **의존성 정책**: 예전에는 새 npm 패키지를 안 늘리는 쪽으로 진행했었는데, 이는 Claude가
  임의로 유추한 스타일이었을 뿐 사용자의 명시적 지침이 아니었다. 확인 결과 **필요하다고
  판단되면 새 의존성을 추가해도 된다**(실제로 이미지 자르기 기능에서 `react-image-crop`을
  추가하며 이렇게 전환함). 다만 꼭 필요한 경우에만 추가하고, 직접 구현이 훨씬 간단한 경우는
  기존처럼 의존성 없이 구현.

## 2. 이미 시도했다가 되돌린 것들 (다시 반복하지 말 것)

- **자정 날짜 갱신**: 처음엔 `pointerdown` 캡처 리스너, 그다음엔 60초 `setInterval` 폴링으로
  구현했었다. 사용자가 "날짜 체크가 너무 자주 되는 것 같다"며 명확히 거부했고, 폴링 없이
  자정 근처(+5초 버퍼)만 정확히 겨냥한 `setTimeout` 재귀 예약 방식(`DateRefresher`)으로
  교체해 확정했다. **같은 종류의 타이머 문제를 다시 만나도 폴링으로 되돌아가지 말 것.**
  단, PWA를 백그라운드에 오래 두면 브라우저/OS가 JS 타이머를 강제로 지연·정지시킬 수 있어
  100% 신뢰는 불가능하다는 걸 사용자에게 이미 설명했고, 이는 받아들여진 상태의 알려진 한계다
  (visibilitychange/focus 이벤트를 폴백으로만 사용).
- **발송예정일 지나면 자동 현물 전환**: PRD 원안은 Vercel Cron이 매일 발송예정일 지난 품목을
  자동으로 현물 전환하는 것이었다. 이후 요구사항이 바뀌어 지금은 그 Cron을 완전히 제거했고
  (`vercel.json`의 `crons` 배열, `api/cron/update-shipped` 라우트 삭제), 대신 "배송중"이라는
  중간 상태를 추가해 사용자가 실제로 받았을 때 "수령 확인" 버튼을 눌러야만 현물로 바뀐다
  (`confirmDelivery` 서버 액션). **PRD.md는 이 부분에서 지금 구현과 어긋나 있으니 참고만 하고
  실제 동작 기준은 코드로 판단할 것.**
- **이미지 자르기 라이브러리**: 처음엔 `react-easy-crop`으로 구현했으나, "회전 폭을 넓히고
  자유 비율로 자를 수 있어야 한다"는 요구사항에는 맞지 않아(그 라이브러리는 고정 비율
  팬/줌 방식) `react-image-crop`으로 교체했다. 회전(1단계)과 자르기(2단계)를 분리한 2단계
  플로우로 설계돼 있다.
- **크롭 좌표 버그**: `react-image-crop`이 기본 제공하는 `cropToCanvas` 헬퍼는 크롭 적용
  시점의 **DOM에 렌더링된 `<img>` 크기**(`image.width`)를 기준으로 스케일을 계산하는데,
  이게 크롭 박스를 그릴 때의 레이아웃과 미묘하게 달라지면 잘라낸 위치/비율이 어긋나는
  버그가 있었다. 그래서 `image.naturalWidth`/`naturalHeight`만 사용하는 커스텀
  `cropImageByPercent`(`src/lib/image-crop.ts`)로 대체했다. **DOM 렌더링 크기에 의존하는
  크롭/좌표 계산은 다시 넣지 말 것.**
- **뒤로가기 스크롤 복원**: Next.js 클라이언트 내비게이션 중 나가는 페이지에서 가짜
  `"scroll"` 이벤트(위치 0 등)가 순간적으로 발생해, 저장 리스너가 방금 저장한 올바른
  위치를 덮어쓰는 문제가 있었다. `pointerdown`은 터치 스크롤 시작에도 발생해서 "이동
  의도"를 구분하는 기준으로 못 쓰고, `a[href]`에 대한 `click` 이벤트만 이동 의도로 보고
  그 이후의 `scroll` 기반 저장은 무시하도록 고쳤다(`src/components/scroll-restoration.tsx`).
  진단 과정에서 화면 위 디버그 로그 오버레이를 임시로 만들어 썼는데(모바일 devtools 접근이
  어려워서), 버그 확정 후 전부 제거했다 — **디버깅용 임시 코드는 확인 후 반드시 제거한다.**

## 3. 코드 스타일 관례

- **주석은 "왜"만, 한글로 짧게**: 무엇을 하는지는 코드/이름으로 충분히 드러나야 하고,
  주석은 숨은 제약·비직관적인 이유·과거에 겪은 버그의 재발 방지용으로만 단다. 여러 줄
  독스트링 블록은 안 씀.
- **서버 액션 패턴**: `src/lib/actions/*.ts`는 `"use server"`로 시작, 각 함수 맨 위에서
  `requireAuth()` 호출, 끝에서 관련된 모든 경로에 `revalidatePath()`(필요시 `redirect()`).
  DB 쿼리 자체(목록 조회 등)는 `"use server"` 없는 `src/lib/*.ts`(예: `items.ts`,
  `shipping-groups.ts`)에 분리해서 둔다. 여러 액션 파일에서 같이 쓰는 순수 로직(매칭 규칙
  등)은 `"use server"` 없는 별도 파일(`src/lib/shipping-group-matching.ts`)로 뺀다.
- **금액은 원 단위 저장, 만원 단위 입력**: `manwonToWon`/`wonToManwon`(`src/lib/money.ts`)로
  변환. 폼에는 항상 만원 단위로 노출.
- **뒤로가기 대상은 `from` 쿼리 파라미터로 전달**: `itemHref(id, from)`,
  `safeRedirectTarget(from)`(`src/lib/nav.ts`) 패턴을 그대로 따른다. 브라우저 히스토리에
  의존하지 않고 항상 "상위 페이지"로 명시적으로 돌아간다.
- **자동보다 사용자 확인을 우선**: 백그라운드에서 조용히 상태를 바꾸는 방식(Cron 자동
  전환 등)은 신뢰성 문제로 걷어냈고, 사용자가 버튼을 눌러 명시적으로 트리거하는 방식
  (수령 확인, 자동 그룹화 다시 실행 등)을 선호한다. 새 자동화를 추가할 땐 이 배경을
  참고해서 "조용한 자동 처리"보다는 "사용자가 트리거하되 결과가 명확히 보이는" 쪽을 우선
  고려할 것.
- **모달/토글은 네이티브 엘리먼트 우선**: `<dialog>`(`m-auto`로 중앙 정렬 이슈 보정),
  `<details>`/`<summary>`를 커스텀 모달/아코디언 라이브러리보다 우선 사용.
- **DOM 렌더링 크기에 의존하지 않기**: 위 크롭 버그처럼, 레이아웃 타이밍에 따라 달라질 수
  있는 값(`element.offsetWidth` 등)에 좌표 계산을 의존시키지 않는다. 가능하면 원본
  데이터(`naturalWidth`, percent 등) 기준으로 계산한다.
- **Prisma 클라이언트 생성 경로가 커스텀**: `src/generated/prisma/client` (스키마 변경 후
  `npx prisma migrate dev --name X` 및 `prisma generate` 필요).

## 4. 이번 세션에서 다룬 기능 (요약)

- 품목 사진 업로드에 회전(1단계)/자유 비율 자르기(2단계) 기능 추가 (`d02fe10`).
- 대시보드 텍스트 줄바꿈/미리보기 잘림 버그 수정 (`8dea7cd`, `9fece66`).
- "배송중" 상태 도입(발송예정일이 지났지만 미수령), 자정 자동 새로고침, 묶음판매에서
  품절 품목 숨김 (`e0e6f44`).
- 자정 갱신을 폴링 대신 정확한 타이머 예약 방식으로 변경 (`92969eb`).
- **발송 그룹** 기능: 같은 날 구매 + 제작자/공구자가 같으면 자동으로 그룹핑, 그룹 단위
  발송일 일괄 수정, 기존 품목 재검사용 백필 버튼. 발송예정일을 "몇 월"까지만 아는 경우도
  입력 가능(월 단위는 달력 칸이 아니라 달력 밑 별도 목록에, 목록 보기에서는 같은 달
  특정일 품목보다 앞에 오도록 정렬), 배송중 상태 옆 날짜 문구를 "발송예정"→"발송일"로
  수정, 발송일 지난 품목 D-Day 색상을 파란색으로 통일 (`72188c1`).

이 이전(품목/판매 기본 CRUD, 카탈로그, 행사 체크리스트 등)은 커밋 메시지(`git log`)를
참고하되, 그 결정들의 배경 설명은 이 문서에 남아있지 않다는 점을 참고할 것.
